CREATE TABLE IF NOT EXISTS "items" (
	"id"	INTEGER,
	"name"	TEXT NOT NULL,
	"category_id"	INTEGER,
	"image_name"	TEXT,
	PRIMARY KEY("id")
);
CREATE TABLE IF NOT EXISTS "categories" (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL
);